/**
 * Пределы размера загружаемого файла в одном месте: их должны знать и
 * приложение, и next.config.ts, а разойтись им нельзя.
 *
 * Предел вложения — то, что видит пользователь. Предел тела запроса берётся
 * с запасом: multipart добавляет к файлу границы частей и заголовки полей.
 */
export const MAX_ATTACHMENT_SIZE = 20 * 1024 * 1024;

/** Запас на служебные поля формы. */
export const REQUEST_BODY_LIMIT = "21mb";

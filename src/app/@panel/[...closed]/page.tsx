/**
 * Любой другой адрес закрывает панель.
 *
 * `default.tsx` срабатывает только при полной загрузке страницы, а переход
 * внутри приложения (в том числе возврат из серверного экшена после
 * сохранения) слот не сбрасывает: без этого пустая форма оставалась висеть
 * поверх уже созданной записи.
 */
export default function PanelClosed() {
  return null;
}
